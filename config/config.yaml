binom:
  base_url: "https://mauritiusdodo.site"
  api_key: "${BINOM_API_KEY}"

admaven:
  base_url: "https://advertisers.ad-maven.com"
  api_token: "${ADMAVEN_API_TOKEN}"

thresholds:
  rule_1:
    min_impressions: 500
    min_bot_rate: 0.30
  rule_2:
    min_impressions: 50
    min_bot_rate: 0.50

lookback_hours: 24
dry_run: true
whitelist_zones: []

notifications:
  enabled: false
  telegram:
    bot_token: "${TELEGRAM_BOT_TOKEN}"
    chat_id: "${TELEGRAM_CHAT_ID}"
