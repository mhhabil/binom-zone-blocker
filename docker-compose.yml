services:
  web:
    build: .
    image: binom-zone-blocker
    command: ["node", "dist/main.js", "--web"]
    env_file: .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./config:/app/config
      - ./logs:/app/logs
    restart: unless-stopped
